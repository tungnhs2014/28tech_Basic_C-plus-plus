#include <bits/stdc++.h>
using namespace std;

// size, length


int main(int argc, char const *argv[])
{
    int x; cin >> x;
    while(x--)
    {
        cin.ignore(1);
        string s;
        getline(cin, s);
        int age;
        cin >> age;
        cout << s << endl;
        cout << age << endl;
    }
    return 0;
}
